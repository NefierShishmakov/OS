Причина вывода segmentation fault, когда массив lines_t объявляется
внутри функции created_pthreads, в том что стэк перезаписался функцией
join_created_pthreads, и когда поток пытается перейти по адресу, где лежала
наша структура, там уже лежит мусор.
